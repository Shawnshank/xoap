pub struct MetalLinkList {
    /*head: None,
    tail: None,
    currentTail: u8,
    maxLen: size,
    marker: PhantomData,*/
}

impl MetalLinkList {
    /*pub fn new(size: u8) -> Self {
        MetalLinkList {
            head: None,
            tail: None,
            currentTail: u8,
            maxLen: size,
            marker: PhantomData,
        }
    }
    pub fn append(&mut self, other: &mut Self) {
        if (self.currentTail + 1) <= self.maxLen{
            self.currentTail += 1;
            self.tail = other.tail;
        } else {
            panic!("Trying to append outside max length of a linked list.");
        }
    }

    pub fn len(&self) -> usize {
        self.len
    }
    pub fn clear(&mut self) {
        *self = Self::new(self.len);
    }*/
}
